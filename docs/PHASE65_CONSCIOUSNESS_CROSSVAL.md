# Phase 65 · 意识交叉验证

> **交叉维度**：EHU 完整栈 × 3840 tick 长时 × 四体信号链；  
> **对照**：同队列关闭 EHU 层。

---

## 一、设计

| 维度 | 实现 |
|------|------|
| EHU | `consciousness_full` 完整栈 |
| 长时 | 3840 tick |
| 多体 | 四体队列 001–004 |
| 信号链 | 初始四体 `socTx` / `socRx` / `socCrossRx` 个体计数 |
| 对照 | `cn_xv_ehu_off_quad` 无 EHU |

运行：`npm run field:phase65`

---

## 二、处理组

| ID | 说明 |
|----|------|
| `cn_xv_quad_3840` | 意识×四体×3840 |
| `cn_xv_ehu_off_quad` | 四体无 EHU 对照 |

---

## 三、假说

| 假说 | 内容 |
|------|------|
| H1 | 多体环境下 H3 份额 ≥ 85% |
| H2 | 四体信号链 2+ 跳可观测 |
| H3 | EHU/LIN/REN 三层迹与信号链并存 |
| H4 | H3 个体与跨位 RX 共现 |
| H5 | 无 EHU 对照 H3≈0 |
| H6 | 无 EHU 时信号链仍可观测 |

---

## 四、观察台

意识面板新增：

- **多体交叉** 徽章（H3 + 跨位 RX 共现）
- 跨位 RX / TX 合计（存活 ≥2 体时显示）

---

## 五、方法论

- 仍**禁止**物种分类与地球式人格名
- 田野用 **StatsRecorder + 个体计数**，禁止全量 TX/RX 日志（3840 tick 会卡死分析）
- 每轮运行前 `resetBirthCounters()`，四体身份证按种子固定
- 分类闸门继续关闭

---

*Phase 65 · 意识收敛中期交叉验证*
