// Auto-generated by scripts/sync-version.mjs — do not edit
const CACHE = 'elecdog-v0.25.0';
const PRECACHE = [
  "./",
  "./index.html",
  "./style.css?v=0.25.0",
  "./src/main.js?v=0.25.0",
  "./manifest.webmanifest",
  "./icons/icon.svg",
  "./src/being/being.js",
  "./src/birth/ritual.js",
  "./src/core/dna.js",
  "./src/core/hash.js",
  "./src/core/id.js",
  "./src/kernel/engine.js",
  "./src/pwa.js",
  "./src/recorder/logger.js",
  "./src/ui/observer.js",
  "./src/ui/stats.js",
  "./src/version.js",
  "./src/world/biotic.js",
  "./src/world/catastrophe.js",
  "./src/world/cell.js",
  "./src/world/composition.js",
  "./src/world/lineage.js",
  "./src/world/nodes.js",
  "./src/world/social.js",
  "./src/world/substrate.js",
  "./src/world/viability.js",
  "./src/world/world.js"
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE).then((cache) => cache.addAll(PRECACHE)).then(() => self.skipWaiting())
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k)))
    ).then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', (event) => {
  if (event.request.method !== 'GET') return;
  const url = new URL(event.request.url);
  if (url.origin !== self.location.origin) return;

  if (event.request.mode === 'navigate') {
    event.respondWith(
      fetch(event.request)
        .then((res) => {
          const clone = res.clone();
          caches.open(CACHE).then((c) => c.put('./index.html', clone));
          return res;
        })
        .catch(() => caches.match('./index.html'))
    );
    return;
  }

  event.respondWith(
    fetch(event.request)
      .then((res) => {
        if (res.ok) {
          const clone = res.clone();
          caches.open(CACHE).then((c) => c.put(event.request, clone));
        }
        return res;
      })
      .catch(() => caches.match(event.request))
  );
});
